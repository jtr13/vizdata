# R packages (combined)

```{r}
pkgs <- readLines("02-new.Rmd") %>% 
  stringr::str_extract("^##.*$") %>% 
  na.omit() %>% 
  stringr::str_remove("## ")

purrr::map_df(pkgs, get_data) %>% 
  show_data(targets = c(5, 10:12))
```
